name: Cross-Platform Build with Py2App and Pyinstaller

# Call when commit is tagged with v*
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  # Creates a release in Github
  create_release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Create Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          draft: true
          prerelease: false
          generate_release_notes: false

  ci_builds:
    # Trigger standalone CI builds after release has been created to upload assets to
    needs: create_release
    runs-on: ubuntu-latest
    name: Trigger CI Builds
    steps:
      - name: Trigger CI builds
        run: 'curl -m 10 --retry 10 --silent --output /dev/null --show-error --fail --json ''{"branch": "main", "variables": {"BUILD_REF": "${{ github.ref_name }}", "PUBLISH_RELEASE": "true"}}'' -X POST -H "Authorization: Bearer ${{ secrets.trigger_ci_build_token }}" "${{ secrets.trigger_ci_build_url }}"'

  build:
    # When create_release is finished
    needs: create_release

    strategy:
      matrix:
        # Building for Mac Intel, Mac ARM
        include:
          - os: macos-x86
            runner: macos-15-intel
            arch: x86_64
          - os: macos-arm64
            runner: macos-latest
            arch: arm64
    # macos-latest is ARM for macOS >= 14
    runs-on: ${{matrix.runner}}

    steps:
      # Checkout the project from Github
      - uses: actions/checkout@v4
      # Get python installed on the runner
      - name: Set up Python
        uses: actions/setup-python@v5
      # Upgrade Pip and install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          arch -${{ matrix.arch }} python -m pip install -r requirements.txt
          arch -${{ matrix.arch }} python -m pip install -r requirements-fork.txt
          arch -${{ matrix.arch }} python -m pip install -r requirements-dev.txt
      # Bring in our apple certificate to the keychain of the runner
      - name: Import Apple Certificate
        if: matrix.os == 'macos-x86' || matrix.os == 'macos-arm64'
        run: |
          if security list-keychains | grep -q "github_build.keychain"; then
            security delete-keychain github_build.keychain
          fi
          security create-keychain -p "" github_build.keychain
          security default-keychain -s github_build.keychain
          security set-keychain-settings -lut 21600 github_build.keychain
          echo "${{ secrets.APPLE_CERTIFICATE }}" | base64 --decode > apple_certificate.p12
          security import apple_certificate.p12 -k github_build.keychain -P "${{ secrets.APPLE_CERTIFICATE_PASSWORD }}" \
            -t cert -f pkcs12 -T /usr/bin/codesign -T /usr/bin/security -T /usr/bin/xcrun
          security unlock-keychain -p "" github_build.keychain
          security set-key-partition-list -S 'apple-tool:,apple:' -s -k "" github_build.keychain
          security list-keychain -d user -s github_build.keychain 'login-keychain'
        env:
          APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
          APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}

      - name: Unlock keychain on Mac
        if: matrix.os == 'macos-x86' || matrix.os == 'macos-arm64'
        run: |
          security unlock-keychain -p "" github_build.keychain
          security set-key-partition-list -S apple-tool:,apple: -k "" -D "Developer" -t private github_build.keychain

      - name: List available signing identities
        if: matrix.os == 'macos-x86' || matrix.os == 'macos-arm64'
        run: |
          security find-identity -v -p codesigning

      - name: Download Sparkle automatic update framework
        run: |
          rm -rf ./resources/Sparkle && mkdir ./resources/Sparkle && curl -L "https://github.com/sparkle-project/Sparkle/releases/download/2.8.1/Sparkle-2.8.1.tar.xz" | tar -xf - -C ./resources/Sparkle

      - name: Build with py2app
        if: matrix.os == 'macos-x86' || matrix.os == 'macos-arm64'
        run: |
          arch -${{ matrix.arch }} python setup.py py2app --arch ${{ matrix.arch }}
        env:
          APPLE_APP_DEVELOPER_ID: ${{ secrets.APPLE_APP_DEVELOPER_ID }}

      - name: Sign application
        if: matrix.os == 'macos-x86' || matrix.os == 'macos-arm64'
        run: |
          cd dist

          # Sign everything that needs signing from the inside out
          find "MarkerMatic.app" -type f \( \
            -iname "*.so" -o \
            -iname "*.dylib" -o \
            -perm -u+x \
          \) | while read f; do
            codesign -s "${{secrets.APPLE_CERTIFICATE_NAME}}" \
              --options runtime --timestamp --force "$f" 2>/dev/null || true
          done

          # Sign nested bundles (.app, .framework, .xpc) from deepest to shallowest
          find "MarkerMatic.app" -type d \( \
            -name "*.app" -o \
            -name "*.framework" -o \
            -name "*.xpc" \
          \) | awk '{ print length, $0 }' | sort -rn | cut -d' ' -f2- | \
          while read bundle; do
            codesign -s "${{secrets.APPLE_CERTIFICATE_NAME}}" \
              --options runtime --timestamp --force "$bundle" 2>/dev/null || true
          done

          # Sign the outer app bundle last
          codesign -s "${{secrets.APPLE_CERTIFICATE_NAME}}" \
            --entitlements ../packaging/entitlements.plist \
            --options runtime --timestamp --force "MarkerMatic.app"

          codesign --verify --verbose=4 "MarkerMatic.app"
          chmod 755 MarkerMatic.app

      - name: Create DMG
        run: |
          chmod a+x "dist/MarkerMatic.app"
          brew update
          brew install create-dmg
          max_tries=10
          i=0
          until create-dmg \
            --volname "MarkerMatic Installer" \
            --app-drop-link 600 185 \
            --window-pos 200 120 \
            --window-size 800 400 \
            --hide-extension "MarkerMatic.app" \
            "MarkerMatic Installer"-${{ matrix.os }}.dmg \
            "dist/MarkerMatic.app"
          do
            if [ $i -eq $max_tries ]
            then
              echo "create-dmg did not succeed even after 10 tries."
              exit 1
            fi
            i=$((i+1))
          done

      - name: Sign DMG
        run: |
          codesign -s "${{ secrets.APPLE_CERTIFICATE_NAME }}" --timestamp --force "MarkerMatic Installer"-${{ matrix.os }}.dmg

      - name: Notarize and staple
        run: |
          xcrun notarytool submit "MarkerMatic Installer-${{ matrix.os }}.dmg" --apple-id \
            "${{ secrets.APPLE_DEVELOPER_ID_USER }}" --password \
            "${{ secrets.APPLE_DEVELOPER_ID_PASSWORD }}" --team-id \
            "${{ secrets.APPLE_DEVELOPER_ID_TEAM }}" --wait --verbose
          xcrun stapler staple "MarkerMatic Installer-${{ matrix.os }}.dmg"

      - name: Verify notarization
        run: |
          spctl -a -t open --context context:primary-signature "MarkerMatic Installer-${{ matrix.os }}.dmg"

      # Add the built binaries to the release
      - name: Release
        uses: softprops/action-gh-release@v2

        with:
          tag_name: ${{needs.create_release.outputs.tag_name}}
          draft: true
          files: |
            MarkerMatic\ Installer-macos-arm64.dmg
            MarkerMatic\ Installer-macos-x86.dmg
